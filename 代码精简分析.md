# 代码模块精简分析

分析对象：本仓库（`hero8152/Infinite-Canvas` 的 fork + `个性化` 分支）。
分析方法：对照作者仓库目录树、静态名称引用扫描、JavaScript/Python 函数规模统计、重复模式统计。

## 总体结论

项目是“单文件后端 + 自包含大 JS 页面”的架构，作者用单文件保证便携部署。精简收益主要集中在三大画布 JS 和 `main.py` 的内部去重；`tools/`、`CLI/`、`python/`、`packages/` 是独立工具或打包依赖，不建议改动。

## 上游仓库对照（2026-08-23）

已对照作者仓库 [hero8152/Infinite-Canvas](https://github.com/hero8152/Infinite-Canvas) 的 `main` 分支最新提交 `1c141a5`（2026-08-04）。本地 `个性化` 分支在其基础上领先 239 个提交，暂未推送。

上游规模实测：`main.py` 17891 行、850 个函数定义、167 个路由、242 处 `except Exception`、119 处 `httpx.` 调用；`smart-canvas.js` 18956 行；`canvas.js` 786KB；`asset-manager.js` 263KB；`api-settings.js` 207KB；`ltx-director-timeline.js` 150KB。

本分支当前规模：`smart-canvas.js` 18784 行、`canvas.js` 15816 行、`asset-manager.js` 4683 行、`api-settings.js` 3802 行、`comfyui-settings.js` 1384 行、`canvas-list.js` 1359 行。

上游仍自带 4 类冗余备份：`smart-canvas.js.broken-before-stable-20260804-171031`、`smart-canvas.js.mojibake-backup`、`smart-canvas.js.stable-before-minimax-20260804-codex`、对应 3 个 CSS 备份，以及 `static/js/i18n(1)/smart-canvas.js` 残留副本。这些备份合计约 2.7MB、数万行，本分支已删除，删除不会影响功能。

独立页面 `angle.html`、`enhance.html`、`gpt-chat.html`、`klein.html`、`online.html`、`zimage.html`、`index.html` 重复同一套主题初始化、Tailwind/lucide 引入和页面外壳，存在模板合并空间；但这是架构级调整，会增大与上游同步的冲突面，只建议在作者仓库侧先做。

`tools/`、`CLI/`、`python/`、`packages/`、`workflows/` 是独立工具、打包依赖或 ComfyUI 工作流配置，不建议纳入精简范围。

## 模块规模

| 模块 | 文件 | 规模 |
| --- | --- | --- |
| 后端 | `main.py` | 18683 行，921 函数，165 路由 |
| 智能画布 | `static/js/smart-canvas.js` | 19023 行，约 1300 函数 |
| 经典画布 | `static/js/canvas.js` | 15989 行，约 1000 函数 |
| 画布列表 | `static/js/canvas-list.js` | 1363 行，约 93 函数 |
| 素材库 | `static/js/asset-manager.js` | 4759 行 |
| API 设置 | `static/js/api-settings.js` | 大页面配置 |
| ComfyUI 设置 | `static/js/comfyui-settings.js` | 大页面配置 |
| LTX 时间线 | `static/js/ltx-director-timeline.js` | 约 150KB |
| 浏览器/PS 插件 | `tools/` | 独立应用 |

## 已推进的精简

- 智能画布已抽取 `smartRequestJson()` 请求封装，替换约 14 处重复的 `fetch(...).then(async r => ...)` JSON 请求解析，行为保持一致，只取 `.detail || fallback`。
- 经典画布已抽取 `canvasRequestJson()` 请求封装，累计替换 15 处重复的 `fetch` + `res.json()` JSON 请求解析，行为保持一致，只取 `.detail || fallback`。
- 画布列表已抽取 `canvasListRequestJson()` 请求封装，替换 7 处重复的 `fetch` + `res.json()` JSON 请求解析，行为保持一致，只取 `.detail || fallback`。
- API 设置已抽取 `apiRequestJson()` 请求封装，替换 12 处重复的 `fetch` + JSON 解析/错误提取。
- 素材管理器已抽取 `apiDownloadBlob()` 下载封装，替换 4 处重复的 blob 下载错误解析。
- ComfyUI 设置已抽取 `comfyRequestJson()` 请求封装，替换 3 处重复的 `fetch` + JSON 解析/错误提取。
- 智能画布/素材管理器已统一多类错误提示入口、图标刷新回调、MiniMax 分段删除与 HTML 渲染辅助，均为独立提交。
- 智能画布提示词分组新增/重命名/删除的错误提示已统一为 toast，避免失败时无反馈。
- 素材管理器和智能画布的 Blob 下载已抽成公共入口下载/复用 `downloadBlob`，减少重复链接创建逻辑。
- 已移除 6 个不再引用的 `broken/mojibake/stable-before` 备份文件。
- `static/js/i18n(1)` 残留副本在当前工作树中未再观察到。
- 已删除 28 个全仓库无引用函数（canvas 9、smart-canvas 6、asset-manager 10、api-settings 2、comfyui-settings 1），净减约 383 行。
- 经典画布 `renderNode` 已拆分为 `canvasNodeTitle`、`renderImageNodeBody`、`bindNodeElementEvents`，由约 232 行降至约 70 行。
- 2026-08-24 经典画布 `renderNode` 进一步拆为 `renderNodeStatusHtml`、`renderNodeHeadHtml`、`bindNodeElementActions`、`appendNodeBody`，主函数由约 113 行降至约 21 行；Playwright 15 类节点渲染与点击选中回归，拆分前后一致且无 `pageerror`。
- 经典画布 `renderMsGenBody` 的事件绑定已拆为 `bindMsModelTabs`、`bindMsCustomModelSelect`、`bindMsRatioResolutionControls`、`bindMsCountInput`、`bindMsLoraControls`、`bindMsSettingChecks`，由约 339 行降至约 135 行（剩余主体为 HTML 模板）。
- 2026-08-24 经典画布 `bindMsRatioResolutionControls` 已拆为 `collectMsSizeElements`、`normalizeMsCustomSizeValues`、`syncMsCustomSizeControls`、`bindMsRatioSelect`、`bindMsResolutionSelect`、`bindMsCustomRatioInputs`、`bindMsCustomSizeInputs`、`bindMsFitSizeButton`，主函数由约 118 行降至约 12 行；Playwright mock 回归覆盖 ratio/resolution/自定义比例/自定义尺寸/fit 按钮/legacy 归一化，拆分前后一致且无 `pageerror`。
- 2026-08-24 经典画布 `renderMiniMaxBody` 已拆为 `miniMaxWorkbenchMetrics`、`miniMaxTimelineTicksHtml`、`miniMaxSegmentsHtml`、`miniMaxRefLaneMetrics`、`miniMaxRefLanesHtml`、`miniMaxRefCounts`、`miniMaxLibraryHtml`、`miniMaxToolbarHtml`、`miniMaxPlayerStageHtml`、`miniMaxTimelineHtml`、`miniMaxCurrentPanelHtml`，主函数由约 103 行降至约 20 行；Playwright mock 回归覆盖 MiniMax 工作台渲染、片段/参考轨/素材库/播放器/超限禁用，拆分前后一致且无 `pageerror`。
- 2026-08-24 智能画布 `smartMinimaxBodyHtml` 已拆为 `smartMinimaxBodyMetrics`、`smartMinimaxTimelineTicksHtml`、`smartMinimaxSegmentsHtml`、`smartMinimaxRefTrackHtml`、`smartMinimaxToolbarHtml`、`smartMinimaxLibraryHtml`、`smartMinimaxTimelineHtml`，主函数由约 114 行降至约 20 行；Playwright mock 回归覆盖智能 MiniMax 工作台渲染、片段/参考轨/素材库/播放器/时间线缩放/静音按钮/导出按钮，拆分前后一致且无 `pageerror`。
- 经典画布 `renderGeneratorBody` 的事件绑定已拆为 `bindGeneratorProviderModel`、`bindGeneratorSizeControls`、`bindGeneratorCountInput`，由约 315 行降至约 90 行。
- 经典画布 `renderLoopBody` 的事件绑定已拆为 `loopRefreshPreview`、`loopRefreshImageHint`、`bindLoopCountAndStartInputs`、`bindLoopModeAndPrompt`、`bindLoopTokens`、`bindLoopImageToggle`、`bindLoopCascadeButtons`，由约 235 行降至约 74 行。
- 经典画布 `renderVideoBody` 的事件绑定已拆为 `bindVideoControls`、`bindVideoActionButtons`，由约 137 行降至约 77 行。
- 经典画布 `bindGeneratorSizeControls` 进一步拆为 `bindGeneratorQualityControls`、`bindGeneratorRatioResolutionControls`、`bindGeneratorFitSizeButton`，由约 184 行降至约 92 行。
- 经典画布 `bindMiniMaxWorkbench` 的事件绑定已拆为 `miniMaxAddRefToSegment`、`miniMaxAssetsForNode`、`miniMaxResolveDroppedItem`、`bindMiniMaxTrackInteractions`、`bindMiniMaxDropZones`、`bindMiniMaxSegmentSelection`、`bindMiniMaxSegmentAddDelete`、`bindMiniMaxPromptAndSelects`、`bindMiniMaxSegmentNumber`、`bindMiniMaxActions`，由约 226 行降至约 27 行。
- 素材管理器 `handleAssetClick` 已按功能拆为 `handleAssetEditActions`、`handleAssetSelectionActions`、`handleAssetItemActions`、`handleAssetUploadTreeActions`、`handleAssetNavigationActions`、`handleAssetCheckCardActions`，由约 146 行降至约 14 行。
- 经典画布 `runComfyNode` 已按模式拆为 `runComfyTextMode`、`runComfyEnhanceMode`、`runComfyCustomMode`、`runComfyEditMode`，外层只剩校验、快照、分发与收尾，由约 159 行降至约 21 行。
- 经典画布 `renderCanvasListInto` 已拆为 `renderCanvasListEmptyState`、`canvasListItemHtml`、`bindCanvasListItemEvents`，由约 120 行降至约 24 行。
- 经典画布 `runNodeCascade` 已拆为 `runCascadeParallelLoop`、`runCascadeSerialLoop`，外层只剩初始化与分发，由约 114 行降至约 23 行。
- 历史图片批量管理 `history-bulk-manager.js` 的 `attach` 已拆为 `historyCards`、`historySelectedCards`、`createHistoryToolbar`、`refreshHistoryToolbar`、`enterHistoryBulkMode`、`exitHistoryBulkMode`、`bindHistoryBulkSelectAll`、`bindHistoryBulkCardSelection`、`deleteSelectedHistoryCards`，主函数由约 140 行降至约 34 行。
- 2026-08-24 页面级回归：Playwright（Chromium 151）打开 `index`、`canvas`、`smart-canvas`、`asset-manager`、`api-settings`、`comfyui-settings`、`canvas-list`、`angle`、`enhance`、`klein`、`online`、`zimage` 共 12 个页面，均 HTTP 200 且无未捕获 JS 异常；唯一 404 为本地 RunningHub workflow 配置缺失，与精简无关。
- 智能画布 `runGeneration` 的成功收尾统一为 `finishSmartGenerationRun`（清空提示词、恢复 settings、scheduleSave 开关参数化），由约 176 行降至约 162 行。
- 智能画布 `runGeneration` 已三段拆分：`prepareSmartGenerationRun` 负责请求准备、`createSmartGenerationPendingState` 负责建 pending 状态、`dispatchSmartGenerationRun` 负责引擎分发与 `try/catch/finally` 收尾，主函数由约 162 行降至 7 行，各段按原顺序串接。
- 智能画布 `runSmartCascade` 已拆为 `smartCascadeRunSetup`、`smartCascadePrepareRunState`、`runSmartCascadeRounds`、`smartCascadeCleanupRun`，主函数由约 210 行降至约 134 行。
- 智能画布 `runSmartCascade` 继续拆出 `runSmartCascadeRound`（单轮执行，约 27 行）、`runSmartCascadeBranch`（分支递归，约 76 行）、`completeSmartCascadeRun`（成功收尾，约 16 行）、`handleSmartCascadeRunError`（失败收尾，约 8 行），主函数由约 134 行降至约 16 行。
- 智能画布 `runCascadeStepIntoNode` 已拆为 `prepareSmartCascadeStepRun`（请求/上下文准备，约 37 行）和 `completeSmartCascadeStepRun`（成功收尾，约 31 行），主函数由约 103 行降至约 30 行，并删除未使用的 `meta` 死代码。
- 智能画布 `runLoopRoundIntoSlot` 已拆为 `prepareSmartLoopRoundRun`（请求/运行状态准备，约 27 行）、`archiveSmartLoopSlotImages`（旧图归档，约 12 行）、`runSmartLoopApiTask`（API 图片任务，约 18 行）、`completeSmartLoopRoundRun`（输出写入与收尾，约 25 行）、`failSmartLoopRoundRun`（失败清理，约 10 行），主函数由约 110 行降至约 36 行。
- 2026-08-24 `runGeneration()` 三段拆分 mock 四分支回归：构造 `smart-image` 节点并写入 `promptInput`，mock `runModelscopeGeneration`/`runApiGeneration`/`runComfyGeneration`，覆盖 modelscope 成功、modelscope 失败、API 图片、ComfyUI 四条路径；拆分前后结果一致且无 `pageerror`。
- 2026-08-24 `runSmartCascade` 二次拆分无循环链路回归：构造 A->B 图片链路并 mock `runCascadeStepIntoNode`，拆分前后均满足选中清空、步骤调用 1 次、运行状态清理、无 `pageerror`。
- 2026-08-24 `runCascadeStepIntoNode` 拆分 mock 回归：构造 A->B 节点并 mock `generateUrlsForCurrentSettings` 返回图片，拆分前后均满足输出写入 1 张、settings 恢复、生成调用 1 次、无 `pageerror`。
- 2026-08-24 `runLoopRoundIntoSlot` 拆分 mock 回归：构造 loop/root/slot 并 mock `generateUrlsForCurrentSettings` 返回图片，拆分前后均满足输出写入 1 张、settings 恢复、生成调用 1 次、runPath 标记 done、无 `pageerror`。
- 经典画布素材管理器 click 回调已拆为 `handleCanvasAssetManagerSelection`、`handleCanvasAssetManagerItemEdit`、`handleCanvasAssetManagerAssetActions`、`handleCanvasAssetManagerWorkflowActions`、`handleCanvasAssetManagerPromptActions`，由约 212 行降至 7 行；拆分后已做 Playwright 页面回归：五个 handler 未命中均返回 `false`，`asset-lib-new` 经真实 modal 监听器触发渲染 1 次，无 `pageerror`。
- 智能画布 `window.onmousemove` 已按拖拽模式拆为 17 个具名 handler（`handleSmartMinimapDragMove`、`handleSmartRightEraseMove`、`handleSmartConnectionEraseMove`、`handleSmartPortDragMove`、`handleSmartPromptResizeMove`、`handleSmartSelectionBoxMove`、`handleSmartPreviewCompareMove`、`handleSmartPanoramaDragMove`、`handleSmartPreviewPanMove`、`handleSmartImageEditPanMove`、`handleSmartCropDragMove`、`handleSmartNodeResizeMove`、`handleSmartLlmInstructionResizeMove`、`handleSmartPromptSplitResizeMove`、`handleSmartThumbDragMove`、`handleSmartPanMove`、`handleSmartNodeDragMove`），`window.onmouseup` 拆为 `handleSmartRightEraseUp`、`handleSmartConnectionEraseUp`、`handleSmartPortDragUp`、`handleSmartMiscDragUp`、`handleSmartNodeDragUp`；两个主回调由 289/197 行降至 17/8 行，并保留“thumbDrag 分离后继续走节点拖拽”“rightErase 无连线时继续清理”两个特殊语义。拆分后 Playwright 回归：19 个 handler 未命中均返回 `false`，pan 移动视口正确、minimap 命中后立即停止分发、rightErase 清理后继续后续清理、无 `pageerror`。
- 2026-08-24 acorn AST 精确扫描：`smart-canvas.js` 的 `runGeneration` 已三段拆分（主函数 7 行）、`runSmartCascade` 已二次拆分（主函数 16 行）、`runCascadeStepIntoNode` 已拆分（主函数 30 行）、`runLoopRoundIntoSlot` 已拆分（主函数 36 行）；`window.onmousemove`/`window.onmouseup` 匿名回调已拆为 17/5 个具名 handler；`canvas.js` 素材管理 click 匿名回调已拆为 5 个具名 handler（13/72/60/29/51 行）；`canvas-list.js`/`asset-manager.js`/`api-settings.js`/`comfyui-settings.js` 无超过 120 行函数；`history-bulk-manager.js` 239 行为 IIFE 模块外壳，内部函数已拆分。剩余超长函数均为核心流程，待真实交互回归后再评估。
- 2026-08-24 经典画布 `runMsGenNode` 已拆为 `prepareMsGenRun`（运行上下文/校验）、`msGenRequestSize`（尺寸计算）、`refreshMsGenRunningState`（运行态刷新）、`collectMsGenImageRefs`（参考图转 base64）、`buildMsGenApiBody`（各模型请求体与 LoRA）、`submitMsGenJob`（级联提交）、`completeMsGenRun`（成功收尾）、`failMsGenRun`（失败/中止收尾），主函数由约 101 行降至约 14 行；Playwright mock 回归覆盖 zimage、qwen_edit、custom+LoRA、失败提示、级联中止 5 条路径，拆分前后一致且无 `pageerror`。
- 2026-08-24 智能画布 `bindNodeThumbSelection` 已拆为 `smartThumbTarget`、`bindSmartThumbMediaItem`、`bindSmartThumbMousedown`、`bindSmartThumbClick`、`bindSmartThumbDblclick`、`bindSmartThumbDragStart`，主函数由约 111 行降至 3 行；Playwright mock 回归覆盖单选、双击、视频、拖拽起点、删除按钮拦截，拆分前后行为一致且无 `pageerror`。
- 2026-08-24 画布列表已清理休眠粘贴 UI（`pasteCanvasBtn`/`clipboardCanvasId`/`updatePasteBtn`/`pasteCanvas`），净删 21 行；Playwright 打开 `canvas-list` 页面无 `pageerror`，无残留符号。对应 HTML 按钮行因与用户未提交版本号改动同文件，保留待确认。
- 2026-08-24 main.py 新增 `save_remote_image_to_output()` 统一 4 处 ModelScope 云端图片下载落盘（cloud_angle×2、cloud、ms），失败回退原 URL、日志行为保持一致，净减 38 行。
- 2026-08-24 main.py 删除 17 个全仓库零引用旧封装函数（`normalize_model_list`、`merge_runninghub_provider_with_static`、`get_best_backend`、`download_image`、`is_video_output_item`、`is_gemini_provider`、`gemini_cli_reference_note`、`provider_supports_avatar`、`find_asset_category`、`is_volcengine_seedance2_model`、`compress_data_url_image`、`valid_video_image_input`、`normalize_apimart_video_reference`、`local_video_path_for_cloud_upload`、`upload_local_video_to_temp_sh`、`volcengine_video_prompt_text`、`runninghub_workflow_store_path`），净删 159 行；py_compile、模块导入、FastAPI TestClient 根路由 200 均通过。
- 2026-08-24 main.py 新增 `modelscope_submit_image_task()` 统一 3 处 ModelScope 生图提交错误处理，新增 `MODELSCOPE_FAILED_STATUSES` 统一 5 处失败状态集合；py_compile、模块导入、TestClient 根路由 200、helper mock 成功/错误路径均通过。
- 2026-08-24 main.py 新增 `upstream_models_endpoint_label()` 统一 3 处上游模型端点标签表达式，删除未使用的 `import sys`。
- 2026-08-24 main.py 新增 `modelscope_poll_task_once()` 统一 4 处 ModelScope 轮询请求与状态解析（ms_generate 保留不 raise/不归一化状态语义），净减 10 行；py_compile、导入、TestClient 根路由 200、helper mock 两种模式通过。
- 2026-08-24 main.py 新增 `read_api_env_entries()` 统一 2 处 `.env` 解析，新增 `ConnectionManager._broadcast_text()` 统一 4 个广播方法的发送与断连清理；错误文本保持一致，净减 16 行；py_compile、导入、TestClient 根路由 200、env/WS mock 通过。
- 2026-08-24 main.py 新增 `cleanup_temp_paths()` 统一 8 处临时文件清理（codex_reference_paths、codex 生图/聊天、gemini-cli 生图/聊天、即梦生图/放大/视频），各清理块保留忽略删除失败语义；py_compile、导入、TestClient 根路由 200、存在/缺失/不可删路径 mock 均通过。
- 2026-08-24 main.py 新增 `save_and_broadcast_image()` 统一 4 处“保存历史 + GLOBAL_LOOP 广播 + 返回”收尾，新增 `chat_assistant_message()` 统一 4 处 codex/gemini-cli 助手消息构造；键与返回结构保持一致，净减 27 行；py_compile、导入、TestClient 根路由 200、helper mock 均通过。
- 2026-08-24 main.py 将 `chat_assistant_message()` 扩展为 `raw_usage` 可选、`raw` 可选，统一剩余 2 处对话/流式助手消息构造；未传 `raw` 的响应保持不出现该键，净减 10 行；py_compile、导入、TestClient 根路由 200、三种消息形态 mock 均通过。
- 2026-08-24 main.py 新增 `volcengine_default_models_payload()` 统一 `fetch_models_from_upstream` 内 4 处方舟默认模型公开载荷转换（3 处带 `protocol`，1 处不带，键顺序保持一致），净减 20 行；py_compile、导入、TestClient 根路由 200、两种载荷形态 mock 均通过。
- 2026-08-24 main.py 新增 `load_chat_conversation()`、`chat_user_message()`、`append_chat_user_message()` 统一 `chat`、`chat_agent`、`chat_stream` 三个端点的会话加载、标题补全与用户消息落库；agent 模式仍显式传 `mode="agent"`，净减 23 行；py_compile、导入、TestClient 根路由 200、会话加载/用户消息 mock 均通过。
- 2026-08-24 main.py 新增 `load_image_asset_category()` 统一 3 处素材库“加载分类并校验图片类型”逻辑（单条添加、批量添加、共享文件夹导入），404/400 错误与原文保持一致，净减 6 行；py_compile、导入、TestClient 根路由 200、图片/非图片/缺失分类 mock 均通过。
- 2026-08-24 main.py 新增 `volcengine_fallback_models_payload()` 统一 `fetch_models_from_upstream` 内 3 处方舟自动探测回退逻辑（`status >= 400` 的 volcengine/openai 分支、HTTPError 分支），消息模板与 `models_error` 字段保持一致，净减 4 行；py_compile、导入、TestClient 根路由 200、命中/未命中回退 mock 均通过。
- 2026-08-24 main.py 新增 `updater_get()` 统一 `fetch_remote_update_notes`/`fetch_remote_version` 两处更新检查 HTTP 请求，请求参数、HTTP 错误文本与 JSON/版本解析分支保持一致；py_compile、导入、TestClient 根路由 200、HTTP 错误/JSON 成功/JSON 异常/空版本/格式异常 mock 均通过。
- 2026-08-24 main.py 新增 `read_env_value()` 统一 7 处 `os.getenv` + `read_api_env_value` 组合读取（codex/gemini-cli/jimeng 三个重复函数及 4 个 provider key helper），删除 3 个相同函数，净减 6 行；py_compile、导入、TestClient 根路由 200、process/env 文件/空值 mock 均通过。
- 2026-08-24 acorn 同文件精确扫描：smart-canvas.js/canvas.js/asset-manager.js/api-settings.js/comfyui-settings.js/canvas-list.js 内无完全相同的函数体；跨文件重复（裁剪/拖放/请求封装等）属于公共模块化架构调整，会影响上游同步，暂不动。
- 2026-08-24 main.py 新增画布后台任务生命周期 helper（create_canvas_task/mark_canvas_task_running/mark_canvas_task_succeeded/mark_canvas_task_failed/get_canvas_task_or_404），统一 run_canvas_image_task/run_canvas_comfy_task 与两个 create/get 端点，保留 jimeng_pending 特判与 upstream_task_id 键差异，净减 13 行；py_compile、导入、TestClient 根路由 200、创建/运行/成功/失败/404 mock 均通过。
- 2026-08-24 main.py 新增 `write_canvas_json()` 统一画布 JSON 落盘；`save_canvas` 仍先刷新 `updated_at`，`update_canvas_meta` 刻意不走 `save_canvas` 的语义保持不变；py_compile、导入、TestClient 根路由 200、落盘不刷新/刷新/元数据更新 mock 均通过。
- 2026-08-24 main.py 拆分 `probe_async_endpoint`（166→31 行）为 `probe_local_cli_endpoint`/`probe_volcengine_async_endpoint`/`async_task_probe_message`/`probe_generic_async_endpoint`，分支、消息、返回结构与错误处理保持一致；py_compile、导入、TestClient 根路由 200、CLI/方舟/消息映射/APIMart/土豆/OpenAI/兼容回退/自动识别/缺参数 mock 25 项均通过。
- 2026-08-24 main.py 拆分 `query_image_task`（120→7 行）为 `query_runninghub_image_task`/`fetch_image_task_raw`/`build_image_task_result`，RunningHub/通用上游/结果构建三段的返回与错误文本保持一致；py_compile、导入、TestClient 根路由 200、RunningHub 成功/805/运行中、拉取成功/HTTP 状态错误/网络错误、结果成功/失败/运行中、主分发 mock 12 项均通过。
- 2026-08-24 main.py 拆分 `update_from_github`（148→62 行）为 `download_update_staging`/`read_staged_update_meta`/`replace_static_update_dir`/`replace_root_update_files`/`apply_staged_update`，下载兜底、版本/更新说明读取、static/root 替换与 root 失败回滚 static 语义保持一致；py_compile、导入、TestClient 根路由 200、锁占用/主源成功/兜底/全失败/元数据/替换/回滚 mock 29 项均通过。
- 2026-08-24 main.py 拆分 `generate_jimeng_video`（119→16 行）为 `jimeng_video_multimodal_args`/`jimeng_video_frames_args`/`jimeng_video_multiframe_args`/`jimeng_video_image_args`/`jimeng_video_text_args`/`build_jimeng_video_args`，多模态/首尾帧/多帧/单图/文生视频五种模式的参数与错误文本保持一致；py_compile、导入、TestClient 根路由 200、五模式/截断/错误/收尾 mock 35 项均通过。
- 2026-08-24 main.py 拆分 `generate`（224→53 行）为 `sync_required_comfy_images`/`load_comfy_workflow`/`apply_comfy_workflow_inputs`/`submit_comfy_workflow`/`wait_for_comfy_history`/`collect_comfy_outputs`，素材同步、工作流装载/参数注入、提交、轮询、输出过滤与最终返回结构保持一致；py_compile、导入、TestClient 根路由 200、同步/装载/注入/提交/轮询/输出/端到端 mock 31 项均通过。
- 2026-08-24 main.py 拆分 `rollback_update`（107→45 行）为 `create_rollback_safety_backup`/`restore_update_backup_static`/`restore_update_backup_root_files`，路径校验、备份状态检查、安全备份与 static/root 还原语义保持一致；py_compile、导入、TestClient 根路由 200、安全备份/static/root/缺失还原/端点错误 mock 30 项均通过。
- 2026-08-24 main.py 拆分 `extract_images`（109→27 行）为 `append_extracted_image`/`collect_extracted_image_value`/`extract_gemini_candidate_images`，URL/base64/image_generation_call/Gemini candidates/result 特判/去重/502 语义保持一致；py_compile、导入、TestClient 根路由 200、URL/base64/Gemini/result 列表/嵌套/去重/无结果 mock 9 项均通过。
- 2026-08-24 main.py 拆分 `generate_runninghub_entry_image`（107→14 行）为 `runninghub_entry_size_field_value`/`runninghub_entry_node_info`/`runninghub_entry_upload_references`/`runninghub_entry_submit_payload`/`runninghub_entry_submit`/`runninghub_entry_poll_output`，尺寸字段、节点参数、上传、提交/轮询与最终返回结构保持一致；py_compile、导入、TestClient 根路由 200、尺寸/节点/上传/负载/提交成功与错误/缺 taskId/轮询成功/无输出/805/超时/主函数编排 mock 28 项均通过。
- 2026-08-24 main.py 拆分 `test_provider_connection`（111→25 行）为 `cli_provider_models_payload`/`jimeng_connection_payload`/`runninghub_connection_payload`/`upstream_connection_credentials`/`upstream_models_redirect_payload`/`upstream_models_html_payload`/`upstream_models_error_payload`/`upstream_models_success_payload`/`upstream_models_http_error_payload`，CLI/即梦/RunningHub 直连分支与通用上游校验、跳转/HTML/错误/成功/HTTPError 语义保持一致；py_compile、导入、TestClient 根路由 200、凭据/CLI/即梦/RunningHub/重定向/HTML/错误/成功/HTTPError/主编排 mock 29 项均通过。
- 2026-08-24 main.py 拆分 `post_openai_responses_stream`（110→64 行）为 `parse_responses_stream_chunk`/`remember_stream_image`/`remember_stream_images_from`/`responses_stream_image_output`/`responses_completed_with_images`/`responses_stream_images_completed`，SSE 事件解析、图片去重收集、completed 图片补写与提前掐断兜底语义保持一致；py_compile、导入、TestClient 根路由 200、事件解析/去重/输出条目/补写/兜底/成功/已有图片/仅图片/错误/无结果/400 回退/500/非 SSE/传输失败 mock 25 项均通过。
- 2026-08-24 main.py 拆分 `chat_stream`（109→29 行）为 `cli_chat_stream_events`/`chat_upstream_messages`/`chat_upstream_stream`，Codex/Gemini CLI 共用 CLI 流事件、通用上游消息组装与 SSE 流语义保持一致；py_compile、导入、TestClient 根路由 200、上游消息/CLI 成功与错误/上游流成功/usage/错误状态/传输失败/Codex/即梦 CLI/通用上游/图片模式 mock 13 项均通过。
- 2026-08-25 main.py 拆分 `generate_ai_image`（244→52 行）为 `normalize_image_generation_quality`/`split_image_reference_lists`/`image_generation_timeout`/`openai_edit_files`/`post_openai_edits`/`post_openai_video_proxy_image`/`post_openai_responses_image`/`post_openai_json_image`/`post_apimart_image`/`post_gpt2_json_image`/`post_openai_edits_image`/`post_plain_generation`/`resolve_image_generation_response`，provider 分发、7 种请求模式、edits 回退/502 与任务轮询语义保持一致；py_compile、导入、TestClient 根路由 200、mock 回归均通过。
- 2026-08-25 main.py 拆分 `canvas_video`（477→107 行）为 `apimart_video_request_body`/`volcengine_video_request_body`/`yuli_video_request_body`/`generic_video_request_body`/`post_canvas_video_json`/`canvas_video_status_error_hint`，provider 分发、APIMart/火山/玉玉/通用请求体、候选端点回退、任务轮询与友好错误提示语义保持一致；py_compile、导入、TestClient 根路由 200、mock 回归 49 项均通过。
- 2026-08-25 ltx-director-timeline.js 死代码扫描：删除零引用常量 `MAX_THUMBNAIL_DIM` 与零引用方法 `growTimelineIfNeeded`；node --check 通过，`beforeRegisterNodeDef` 为 ComfyUI 框架钩子保留。
- 2026-08-25 全量前端 JS 死代码扫描：删除 `asset-manager.js` 的 `PROMPT_BUILTIN_CATEGORY_IDS`、`canvas.js` 的 `LTX_SEGMENT_COLORS`/`QUICK_TOOLBAR_COLLAPSED_KEY`/`entryNote`、`smart-canvas.js` 的 `runBtnCooldownToken`/`activeCatId`；node --check 通过，剩余 `beforeRegisterNodeDef` 为 ComfyUI 框架钩子保留。
- 2026-08-25 main.py 零引用清理：删除 6 个常量与 8 个函数（`APP_VERSION`、`MODELSCOPE_RAW_ROOT`、`RUNNINGHUB_OPENAPI_BASE_URL`、`LINGJING_DEFAULT_BASE_URL`、`MODELSCOPE_DEFAULT_IMAGE_MODEL`、`MODELSCOPE_DEFAULT_CHAT_MODEL`、`probe_async_endpoint`、`query_image_task` 及其 6 个下游 helper）；py_compile、导入、TestClient 根路由 200、AST 复扫 0 候选均通过，净减 271 行。
- 2026-08-25 main.py 零引用请求模型清理：删除 `TokenRequest`、`ImageTaskQueryRequest` 两个仅定义无引用的 Pydantic 模型；py_compile、导入、TestClient 根路由 200、类引用复扫 0 候选均通过，净减 7 行。
- 2026-08-25 main.py HTTP 同构小簇：新增 `raise_chat_upstream_status_error()` 统一 4 处 `friendly_chat_error_detail` 状态错误抛错；py_compile、导入、TestClient 根路由 200、helper 冒烟（429/文案）均通过，净减 3 行。
- 2026-08-25 main.py HTTP 同构小簇（生图）：新增 `raise_chat_image_http_error()` 合并 2 处“状态错误+网络错误”双 except 为单 helper；py_compile、导入、TestClient 根路由 200、helper 双分支冒烟（429/502）均通过，main.py 净减 2 行。
- 2026-08-25 canvas.js 新增 `assertCanvasTaskResponse()` 统一 3 处任务轮询的 404 重启提示+错误断言；node --check 通过，净减 2 行。
- 2026-08-25 前端剩余 `.ok` 分支评估完成：`smart-canvas.js`/`canvas-list.js`/`asset-manager.js`/`api-settings.js` 的剩余分支为静默返回、409 冲突合并、自定义 JSON 错误或单行抛错，暂不抽取。
- 2026-08-25 全量复核：main.py py_compile、6 个前端 JS node --check、导入+TestClient 根路由/静态页 200、路由数 172 均通过。
- 2026-08-25 上游同步核验：`hero8152/Infinite-Canvas` main 仍为 `1c141a5`，与文档基线一致，暂无新上游提交需要同步。
- 2026-08-25 main.py HTTP 同构小簇（聊天）：新增 `raise_chat_upstream_http_error()` 合并 4 处“状态错误+网络错误”双 except 为单 helper（其中 1 处保留网络错误日志）；py_compile、导入、TestClient 根路由 200、helper 双分支冒烟（429/502）+ log 分支均通过，main.py 净减 2 行。
- 2026-08-25 main.py HTTP 同构小簇（视频）：新增 `raise_video_provider_http_error()` 合并 5 处“状态错误+网络错误”双 except 为单 helper（土豆分支保留友好提示）；py_compile、导入、TestClient 根路由 200、helper 四分支冒烟（普通/友好/回退/网络日志）均通过，main.py 净减 10 行。
- 2026-08-25 canvas.js 抽取 `completeCanvasRun()` 统一 RunningHub/Video/MiniMax/LTX Director/Comfy 五处生成成功收尾；node --check、git diff --check 通过，净减 26 行。
- 2026-08-25 canvas.js 抽取 `finishCanvasPendingOutput()` 统一 `completeRecoverPendingOutput`/`completeCanvasImageTask` 两处任务成功收尾；node --check、git diff --check 通过，净减 15 行。
- 2026-08-25 comfyui-settings.js 抽取 `previewSliderHtml()`/`previewDropdownHtml()` 统一 `renderPreviewField`/`renderMiniField` 两处滑块与下拉框 HTML；node --check、git diff --check 通过，净减 7 行。
- 2026-08-25 canvas-list.js 抽取 `appendCanvasToBoard()` 统一新建/导入/复制三处画布入列与刷新；node --check、git diff --check 通过，净减 15 行。
- 2026-08-25 asset-manager.js 抽取 `applyLocalUploadFolderData()` 统一本地素材文件夹创建/重命名的状态刷新；node --check、git diff --check 通过，净减 3 行。
- 2026-08-25 smart-canvas.js 抽取 `runComfyCustomWorkflow()` 统一 `generateComfyUrlsWithSettings`/`runComfyGeneration` 两处自定义工作流字段构建与提交；node --check、git diff --check 通过，净减 18 行。
- 2026-08-25 canvas.js 抽取 `aspectRatioOptions()` 统一 ms/generator/rh-model 三处 9 项比例下拉选项；node --check、git diff --check 通过，净减 14 行。

## 各模块精简机会

### 1. 智能画布 `smart-canvas.js`（收益最高）

- 原清单的 40 个零引用函数已大部分清理（部分仍被 HTML onclick 引用），主要分四类：
  - 旧尺寸/比例/分辨率渲染器：`renderSizeControls`、`renderRatioControl`、`renderResolutionControl`、`renderCustomRatioControls`、`renderCustomSizeControls`、`renderInlineCustomRatioFields`、`renderInlineCustomSizeFields`、`renderCountControl`、`renderManualVideoUrlControl`、`renderTempShUploadControl`，已被 `sizePicker*` 系列替代。
  - 半成品功能 helper：网格拼接（`openGroupGridJoin`、`resetGridJoinLayout`、`toggleGridCustomMode` 等）、全景预览（`isLikelyPanoramaImage`、`togglePanoramaPreview`）、笔刷与撤销（`setBrushTool`、`undoEditDrawing`、`redoEditDrawing`）。
  - 旧数据 helper：`activeInputImagesFor`、`connectedLineNodeIds`、`coolRunButton`、`defaultInputImagesFor`、`outgoingInputConnectionsFor`、`referenceImagesFor`。
  - 提示词/素材相关：`createPromptPresetFromComposer`、`savePromptNodeAsPreset`、`currentComposerSubject`、`mentionCandidateImages`、`smartLoopPromptFieldText` 等。
  - 本批再删 5 个无引用函数：`optionHtml`、`ratioIconClass`、`disconnectConnection`、`candidateInputImagesFor`、`isInputRefBlocked`。
  - 2026-08-25 复核：`nodeHasReferenceContent` 已在 `4d6bf77` 删除，当前全仓库 0 引用，原待办关闭。
  - 画笔/撤销/网格/全景按钮仍被 `smart-canvas.html` 的 onclick 直接引用，若继续清理需同步改用户未提交的 HTML，暂不动。
- `bindMinimaxNodeControls`（54 行）、`bindNodeEvents`（18 行）、`bindDynamicParams`（9 行）此前已拆分；`runGeneration` 已三段拆分为 `prepareSmartGenerationRun`、`createSmartGenerationPendingState`、`dispatchSmartGenerationRun`（主函数 7 行，成功收尾统一为 `finishSmartGenerationRun`）；`runSmartCascade` 已拆为准备/单轮/分支递归/收尾 helper（主函数约 16 行），核心递归 `runSmartCascadeBranch` 独立为顶层函数；`runCascadeStepIntoNode` 已拆为准备/收尾 helper（主函数约 30 行）；`runLoopRoundIntoSlot` 已拆为准备/API 任务/收尾/失败 helper（主函数约 36 行）；`window.onmousemove`/`window.onmouseup` 已拆为 17/5 个具名模式 handler，主回调降至 17/8 行。
- 智能画布 `smartMinimaxBodyHtml` 当前主函数约 20 行，由 7 个新 helper 承载指标、刻度、片段、参考轨、工具栏、素材库与时间线 HTML；播放器/当前片段/素材列表 helper 为此前已有。
- 智能画布新增 `assertSmartCanvasResponseMessage()` 统一 6 处 `smartResponseErrorMessage` 错误抛出；剩余 1 处 `response.text()` 原始文本（云端视频上传探测）语义不同，保留。
- 智能画布新增 `assertSmartCanvasResponseText()` 统一 12 处失败时抛响应原文的错误处理，其中 2 处带 fallback 的行为保持不变。
- 智能画布新增 `smartRunningHubJson()` 统一 RunningHub 提交/查询两处的 JSON 解析、payload 级错误抛出与返回值；执行阶段 `FAILED` 判断保留在原流程。
- 智能画布新增 `smartRequestJsonText()` 统一 11 处失败抛响应原文的 JSON 请求，新增 `smartRequestJsonMessage()` 统一 2 处失败抛详细消息的 JSON 请求。
- 与 `canvas.js`、`canvas-list.js` 重复的视口数学 `screenToWorld` 等，抽公共模块属于架构调整，影响上游同步。
- 智能画布新增 `runComfyCustomWorkflow()` 统一 `generateComfyUrlsWithSettings`/`runComfyGeneration` 两处自定义 Comfy 工作流的字段构建与提交（prompt/media/setting + workflow-custom），各自结果处理保留；node --check、git diff --check 通过，净减 18 行。

### 2. 经典画布 `canvas.js`

- 零引用函数已全部删除：`computeConnectedWorkflowOrder` 已被 `computeCascadeOrder` 替代；`resolveProviderId` 已被 `resolveImageProviderId`/`resolveChatProviderId` 替代；`startSelectionLink` 没有入口；`normalizeProviderId`、`splitPromptIntoItems`、`msChatModelOptions` 等无引用。原 10 个在 `a0ff596` 与本批（9 个）全部删除。
- 经典画布大函数已全部拆分完成：`renderNode`、`renderGeneratorBody`、`renderLoopBody`、`renderVideoBody`、`bindGeneratorSizeControls`、`bindMiniMaxWorkbench`、`runComfyNode`、`renderCanvasListInto`、`runNodeCascade`、`renderPromptTemplateModal`、`renderMsGenBody` 均已拆分；其中 `renderMsGenBody` 拆为 `msModelTabsHtml`、`msContentHtml`、`msControlsHtml`，主函数由约 134 行降至约 33 行，并移除未使用变量 `loraEnabled`。当前经典画布已无超过 120 行的具名函数；素材管理 click 匿名回调也已按选择、条目编辑、资产操作、工作流操作、提示词操作拆为 5 个具名 handler（13/72/60/29/51 行），主回调降至 7 行。
- 经典画布 `renderNode` 当前主函数约 21 行，由 `renderNodeStatusHtml`、`renderNodeHeadHtml`、`bindNodeElementActions`、`appendNodeBody` 四个 helper 承载类型化渲染。
- 经典画布 `bindMsRatioResolutionControls` 当前主函数约 12 行，由 8 个控件 helper 承载元素收集、旧值归一化、控件同步与各控件事件绑定。
- 经典画布 `renderMiniMaxBody` 当前主函数约 20 行，由 11 个 MiniMax 工作台 helper 承载尺寸指标、刻度/片段/参考轨/素材库 HTML、工具栏/播放器/时间线/当前片段面板与参考计数。
- 经典画布 `runMsGenNode` 当前主函数约 14 行，由 `prepareMsGenRun`、`msGenRequestSize`、`refreshMsGenRunningState`、`collectMsGenImageRefs`、`buildMsGenApiBody`、`submitMsGenJob`、`completeMsGenRun`、`failMsGenRun` 八个 helper 承载上下文准备、尺寸、运行态、参考图、请求体、提交与收尾。
- 经典画布已新增 `assertCanvasResponseOk()` 统一 10 处纯 `throw new Error(...)`，新增 `assertCanvasResponseMessage()` 统一 18 处 `responseErrorMessage` 错误抛出；其余约 33 处 `.ok` 分支仍含 payload `success === false`、`return null`、404 特判等语义，需分类评估。
- 经典画布新增 `canvasRunningHubJson()` 统一普通 RunningHub 提交/查询两处，新增 `miniMaxRunningHubJson()` 统一 MiniMax RunningHub 提交/查询两处；执行阶段 `FAILED` 判断保留在原流程。
- 经典画布新增 `canvasResponseJsonMessage()` 统一视频生成、ComfyUI 上传、LLM 调用 3 处失败抛详细消息的 JSON 请求收尾。
- 经典画布新增 `completeCanvasRun()` 统一 RunningHub/Video/MiniMax/LTX Director/Comfy 五处生成成功收尾（pending 清理、meta 收集、输出写入、级联合并、日志、状态刷新、自动保存）；node --check、git diff --check 通过，净减 26 行。
- 经典画布新增 `finishCanvasPendingOutput()` 统一 `completeRecoverPendingOutput`/`completeCanvasImageTask` 两处任务成功收尾（meta 收集、pending 清理、输出写入、日志、状态刷新、自动保存），空结果守卫按原调用方保留；node --check、git diff --check 通过，净减 15 行。
- 经典画布新增 `aspectRatioOptions()` 统一 `msControlsHtml`/`renderGeneratorBody`/`rhModelSettingsHtml` 三处比例下拉选项，source/custom 差异通过参数保留；node --check、git diff --check 通过，净减 14 行。

### 3. 画布列表 `canvas-list.js`

- 已清理：`escapeAttr`、`cutCanvas`、重复下载逻辑（`downloadBlob`）、重命名输入逻辑（`attachInlineInput`）、回收站角标（`updateTrashBadge`）。
- 已抽取 `canvasListRequestJson()` 统一 7 处 JSON 请求，新增 `assertCanvasListResponseOk()` 统一 5 处简单错误抛出；剩余分散 `.ok` 处理（含导入/复制画布的自定义错误解析、blob 下载、无 JSON 响应接口），已于 2026-08-25 评估为暂不抽取。
- 已清理：`pasteCanvasBtn`/`clipboardCanvasId`/`updatePasteBtn`/`pasteCanvas` 休眠粘贴 UI；对应 HTML 按钮行仍保留（工作树有用户未提交的版本号改动，未一并修改）。

- 2026-08-25 新增 `appendCanvasToBoard()` 统一新建/导入/复制三处画布入列与刷新；node --check、git diff --check 通过，净减 15 行。

### 4. 素材库 `asset-manager.js`

- 零引用函数已全部删除：`canvasAssetOpenUrl`、`localChildPath`、`localFolderId`、`normalizeAssetMoveTarget`、`renderLocalCard`、`renderLocalDetail`，原列 6 个与后续新增发现的共 10 个。
- 已有 `apiJson()` JSON 请求封装，本批新增 `apiDownloadBlob()` 统一 4 处打包下载；剩余分散 `.ok` 多为素材条目级 `ok` 字段判断。
- `handleClick` 已拆分为多个命名 handler（当前 38 行），`handleAssetClick` 也已拆为 6 个命名 handler；`renderStorageSettingsModal` 已拆为 `storageDirRowsHtml`、`storageTabsHtml`、`storageFileCardsHtml`、`storagePrefsBodyHtml`、`storageManageBodyHtml`，主函数由约 142 行降至约 36 行。当前 asset-manager.js 已无超过 120 行的函数。

- 2026-08-25 新增 `applyLocalUploadFolderData()` 统一本地素材文件夹创建/重命名的状态刷新；node --check、git diff --check 通过，净减 3 行。

### 5. API 设置 `api-settings.js`

- 零引用函数已全部删除：`applyProviderOnboardingDefaults`、`focusRhWorkflowEditorNode`、`handleRhPasteInput`、`hasLockedRecommendedProtocol`、`renderRhWorkflowEditorNodeList`，共 5 个（本批删 2 个，其余此前已清理）。
- 已抽取 `apiRequestJson()` 统一 12 处 JSON 请求；剩余约 12 处分散 `.ok` 处理（含 RunningHub payload 级 `success === false` 校验），已于 2026-08-25 评估为静默返回、409 冲突合并、自定义 JSON 错误或单行抛错，暂不抽取。
- `renderEditor` 已按平台拆为 `applyRunningHubEditorState`、`applyVolcengineEditorState`、`applyJimengEditorState`、`applyCodexEditorState`、`applyGeminiCliEditorState`，主函数由约 142 行降至约 94 行。
- `renderProviderOnboarding` 已拆为 `modelscopeOnboardingHtml`、`runningHubOnboardingHtml`，主函数由约 102 行降至约 24 行。
- `saveProviders` 已拆为 `normalizeProviderForSave`、`providerPayloadForSave`、`clearProviderSecrets`，主函数由约 115 行降至约 30 行。
- API 设置新增 `apiRunningHubJson()` 统一 RunningHub 预览提交/查询两处；`loadRhWorkflowEditorConfig` 的 404 特判保留。

### 6. ComfyUI 设置 `comfyui-settings.js`

- 1 个零引用函数 `miniLine`（文档此前误写为 `miniCardStyle`）已删除，另有 1 处注释掉的代码。
- 已抽取 `comfyRequestJson()` 统一 3 处 JSON 请求；剩余 2 处 `.ok` 为不解析 JSON 的保存/删除接口。
- 已抽取 `comfyRequestJson()` 统一 5 处 JSON 请求（含保存/删除接口）；当前文件内无剩余分散 `.ok` 分支。
- 2026-08-25 新增 `previewSliderHtml()`/`previewDropdownHtml()` 统一 `renderPreviewField`/`renderMiniField` 两处滑块/下拉框 HTML；node --check、git diff --check 通过，净减 7 行。

### 7. 后端 `main.py`

- 后端已无超过 200 行的函数：`canvas_video`（477→107 行）已拆为 `apimart_video_request_body`/`volcengine_video_request_body`/`yuli_video_request_body`/`generic_video_request_body`/`post_canvas_video_json`/`canvas_video_status_error_hint`；此前 `probe_async_endpoint`/`query_image_task`/`update_from_github`/`generate_jimeng_video`/`generate`/`rollback_update`/`extract_images`/`generate_runninghub_entry_image`/`test_provider_connection`/`post_openai_responses_stream`/`chat_stream`/`generate_ai_image` 均已拆分。
- 232 个 `except Exception`、58 处 `async with httpx.AsyncClient`、40 处 `raise_for_status()`；2026-08-25 按成对 `HTTPStatusError+HTTPError` 逐簇评估，已抽取聊天 4 处、视频 provider 5 处；剩余块错误提示/日志仍高度个性化，不再强抽公共包装。
- 2026-08-25 同构小簇评估完成：`raise_chat_upstream_status_error`/`raise_chat_image_http_error`/`raise_chat_upstream_http_error`/`raise_video_provider_http_error` 已抽取；剩余精确重复块多为 1-2 行（`except Exception: pass`、`return {}`、单行 502 抛错等），抽 helper 净增行数或增加间接层，维持不抽取结论。
- 不建议拆成多文件包：作者用单文件保证便携部署，拆包会破坏用户直接运行 `main.py` 的习惯，且与上游同步冲突风险高。

### 8. 冗余文件

- `static/js/i18n(1)/smart-canvas.js` 与 `static/js/i18n/smart-canvas.js` 内容差 14 行，是残留副本；已确认当前工作树不存在 `i18n(1)` 残留副本。
- 上游自带 6 个 `broken/mojibake/stable-before` 历史备份（共 57834 行），不是功能模块；本地 `6c1ad44` 已全部删除，上游仓库仍保留，以后同步上游时需注意会重新出现。

### 9. 上游模块分区（2026-08-25 实测）

| 区域 | 上游规模 | 精简判断 |
| --- | --- | --- |
| `main.py` | 17891 行/880KB | 可做：大函数拆分、HTTP/异常包装，本地已推进 |
| `static/` | HTML/CSS/JS 前端 | 可做：大函数、请求包装、死代码清理，本地已推进 |
| `CLI/` | 21 文件/8.8MB | 不建议动：独立 CLI 工具，与画布同步冲突 |
| `python/` | 34 文件/16.7MB | 不建议动：内置运行环境/依赖 |
| `packages/` | 22 文件/11.1MB | 不建议动：打包依赖 |
| `tools/` | 26 文件/263KB | 不建议动：浏览器/PS 插件 |
| `workflows/` | 9 文件/44KB | 不建议动：示例工作流 JSON |
| `tests/` | 15KB | 上游测试，本地分支未保留 |
| `data/`、`API/` | 数据/占位 | 无需精简 |

- 本地 `main.py` 当前无超过 200 行的函数；`canvas_video` 已由 477 行拆至 107 行，下一步可继续处理 HTTP/异常包装。
- 全量前端 JS 已完成死代码扫描；当前仅剩 `beforeRegisterNodeDef` 一个 ComfyUI 框架钩子候选，不能删。

## 优先级建议

### P0（低风险，确认后可做）

- `tests/test_canvas_log_cleanup.py` 在当前工作树已不存在，`tests/` 目录未随本分支保留，原失配测试项已失效，无需处理。
- 零引用函数清理（已完成大部分）：27 个零引用函数分批删除并逐批提交，`i18n(1)` 残留副本确认不存在。
- 已完成：`canvas-list.js` 的休眠粘贴 UI JS 侧已清理；`static/canvas-list.html` 的 `pasteCanvasBtn` 按钮行因与用户未提交的版本号改动同文件，保留待确认。

### P1（中风险，需要回归验证）

- 抽出公共的 fetch/错误提示/状态提示包装，减少各页面重复。
- 后端 HTTP/异常包装：AST 扫描的成对异常块已按真正同构小簇抽取（聊天 4 处、视频 provider 5 处）；剩余块文案/日志不同，暂不统一包装。

### P2（高风险，不建议现在做）

- `main.py` 拆包。
- 前端公共模块化，影响上游同步。

## 验证方式

- JavaScript：`node --check`。
- Python：`python -m py_compile main.py`。
- 页面：通过运行中的 `http://127.0.0.1:3000/` 访问各静态资源确认 200。
- 页面级（可选）：环境带 Playwright 时，用 Chromium headless 打开各静态页并收集 `pageerror`/console error。
- 测试：当前 `tests/test_canvas_log_cleanup.py` 因上游移除 `delete_canvas_log` 全部失败，作为存量问题单独处理。
